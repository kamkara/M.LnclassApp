 <% course.exercices.chrono.each do |q| %>
    <% if q.completed_by(current_user) %>
        <%= render partial: "courses/shared/completed_exercices", locals: {q: q} %>
    <% else %>
        <%= render partial: "courses/shared/unattempted_exercices", locals: {q: q} %>
    <% end %>
<% end %>
